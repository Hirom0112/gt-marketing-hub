# S14 mock apply SPA — client env (Vite injects VITE_* at build).
# ONLY the Supabase anon key belongs here (INV-5: service_role is server-only and
# must NEVER appear in a VITE_* var or this client bundle).
VITE_SUPABASE_URL=https://kgyzptpzccoczbruhvow.supabase.co
VITE_SUPABASE_ANON_KEY=<the gt-apply-demo anon key>
